name: application

services:
  qinglong:
    image: "whyour/qinglong:2.19.2"
    container_name: "qinglong"
    volumes:
      - ./data:/ql/data
    ports:
      - "5700:5700"
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 512M
    logging:
      driver: "json-file"
      options:
        max-size: "2m"
        max-file: "1"